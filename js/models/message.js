var app = app || {};

app.Message = Backbone.Model.extend({
    defaults: {
        content: '',
    }

    //initialize: function( options ) {
        //console.log( 'message model: new message ' + this);
    //}
});